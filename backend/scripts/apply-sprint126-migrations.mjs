/**
 * Alias histórico retirado.
 *
 * Las migraciones se ejecutan únicamente mediante migrate-official.mjs.
 */
console.error('apply-sprint126-migrations.mjs está deprecado; use npm run db:migrate');
process.exitCode = 1;
