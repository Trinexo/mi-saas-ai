import { apiRequest } from './api';

export const testApi = {
  generate: (token, payload) => apiRequest('/tests/generate', { method: 'POST', body: payload, token }),
  generateDemo: (token, oposicionId) => apiRequest('/tests/generate-demo', { method: 'POST', body: { oposicionId }, token }),
  generateRefuerzo: (token, payload) => apiRequest('/tests/generate-refuerzo', { method: 'POST', body: payload, token }),
  getRecommended: (token, oposicionId) => apiRequest('/tests/recomendado', { token, query: oposicionId ? { oposicion_id: oposicionId } : {} }),
  getContinuar: (token, oposicionId) => apiRequest('/tests/continuar', { token, query: oposicionId ? { oposicion_id: oposicionId } : {} }),
  getPendientes: (token, oposicionId, options = {}) => apiRequest('/tests/pendientes', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  cerrar: (token, testId) => apiRequest(`/tests/${testId}/cerrar`, { method: 'POST', token }),
  getFocoHoy: (token) => apiRequest('/stats/foco-hoy', { token }),
  getResumenSemana: (token, oposicionId, options = {}) => apiRequest('/stats/resumen-semana', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getActividad14Dias: (token, oposicionId, options = {}) => apiRequest('/stats/actividad-14dias', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getTemasDebiles: (token, oposicionId) => apiRequest('/stats/temas-debiles', { token, query: oposicionId ? { oposicion_id: oposicionId } : {} }),
  getProgresoTemas: (token, oposicionId, options = {}) => apiRequest('/stats/progreso-temas', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getProgresoTemasReal: (token, oposicionId, options = {}) => apiRequest('/stats/progreso-temas-real', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getInsightMensual: (token, oposicionId, options = {}) => apiRequest('/stats/insight-mensual', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getProgresoSemanal: (token, oposicionId, options = {}) => apiRequest('/stats/progreso-semanal', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getEficienciaTiempo: (token, oposicionId, options = {}) => apiRequest('/stats/eficiencia-tiempo', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getRitmoPregunta: (token, oposicionId, options = {}) => apiRequest('/stats/ritmo-pregunta', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getBalancePrecision: (token, oposicionId, options = {}) => apiRequest('/stats/balance-precision', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  submit: (token, payload) => apiRequest('/tests/submit', { method: 'POST', body: payload, token }),
  history: (token, params = {}) => apiRequest('/tests/history', { token, query: params }),
  userStats: (token, oposicionId, options = {}) => apiRequest('/stats/user', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  temaStats: (token, bloqueId) => apiRequest('/stats/bloque', { token, query: { bloque_id: bloqueId } }),
  repasoStats: (token, bloqueId) => apiRequest('/stats/repaso', { token, query: { bloque_id: bloqueId } }),
  simulacroStats: (token, oposicionId, options = {}) => apiRequest('/stats/simulacros', { token, query: { oposicion_id: oposicionId, ...options } }),
  getDashboard: (token, oposicionId, options = {}) => apiRequest('/stats/dashboard', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getObjetivoDiario: (token, oposicionId, options = {}) => apiRequest('/stats/objetivo-diario', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getGamificacion: (token, oposicionId, options = {}) => apiRequest('/stats/gamificacion', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getRacha: (token, oposicionId, options = {}) => apiRequest('/stats/racha', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getRachaBloques: (token) => apiRequest('/stats/racha-bloques', { token }),
  getResumenOposicion: (token, oposicionId, options = {}) => apiRequest('/stats/resumen-oposicion', { token, query: { oposicion_id: oposicionId, ...options } }),
  getProgresoBloques: (token, oposicionId, options = {}) => apiRequest('/stats/progreso-bloques', { token, query: { ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getProgresoBloquesByTema: (token, temaId, options = {}) => apiRequest('/stats/progreso-bloques-tema', { token, query: { tema_id: temaId, ...options } }),
  getProgresoTemaReal: (token, temaId, options = {}) => apiRequest('/stats/progreso-tema-real', { token, query: { tema_id: temaId, ...options } }),
  getDetalleBloque: (token, bloqueId, options = {}) => apiRequest(`/stats/bloque/${bloqueId}/detalle`, { token, query: options }),
  getMisOposiciones: (token) => apiRequest('/stats/mis-oposiciones', { token }),
  getRanking: (token, oposicionId) => apiRequest('/stats/ranking', { token, query: oposicionId ? { oposicion_id: oposicionId } : {} }),
  evolucionStats: (token, limit = 30, oposicionId, options = {}) => apiRequest('/stats/evolucion', { token, query: { limit, ...(oposicionId ? { oposicion_id: oposicionId } : {}), ...options } }),
  getRepasoPendientes: (token, limit = 20, oposicionId = null) =>
    apiRequest('/repaso/pendientes', { token, query: { limit, ...(oposicionId ? { oposicion_id: oposicionId } : {}) } }),
  getReview: (token, testId) => apiRequest(`/tests/${testId}/review`, { token }),
  getConfig: (token, testId) => apiRequest(`/tests/${testId}/config`, { token }),
};
